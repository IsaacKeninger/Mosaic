export default function DashboardPage() {
  // TODO: wire up UserSwitcher + usePersona hook, then render
  // PersonaCard, SpendingBreakdown, PersonaScatter, PeerComparison,
  // and BedrockInsights for the selected user.
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-semibold">Mosaic</h1>
      <p className="text-sm text-gray-500">Dashboard scaffold — components not yet wired up.</p>
    </main>
  );
}
